# Bland AI API key — create one at https://app.bland.ai (Settings -> API Keys).
# Used only by the Bun proxy (server.ts); it is never bundled into the client.
# Leave unset to run the app in mock mode.
BLAND_API_KEY=
