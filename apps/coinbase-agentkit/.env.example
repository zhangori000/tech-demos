# Only needed for optional LIVE mode (bun run server).
# Get these from https://portal.cdp.coinbase.com → API keys (Secret API Key)
# and Wallet Secret. The mock-mode SPA needs none of them.
CDP_API_KEY_ID=your-api-key-id
CDP_API_KEY_SECRET=your-api-key-secret
CDP_WALLET_SECRET=your-wallet-secret
